{
  "name": "ai-intent-aware-sales-intelligence-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise-grade AI-powered Sales Intelligence Platform with behavior tracking, Customer DNA profiling, and real-time voice sales conversations.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n FRONTEND,BACKEND -c cyan,magenta \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "build": "npm run build --workspace=frontend",
    "start:backend": "npm run start --workspace=backend",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=backend",
    "prisma:generate": "npm run prisma:generate --workspace=backend",
    "prisma:migrate": "npm run prisma:migrate --workspace=backend",
    "prisma:studio": "npm run prisma:studio --workspace=backend",
    "postinstall": "echo 'Run npm run prisma:generate after configuring backend/.env'"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "overrides": {
    "langsmith": "^0.8.4"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "license": "UNLICENSED",
  "author": "Eubrics"
}
